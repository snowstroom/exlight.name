import { Component, Input } from '@angular/core';
import { FormControl } from '@angular/forms';

@Component({
    selector: 'ex-input',
    templateUrl: 'exl-input.component.html',
    styleUrls: ['exl-input.component.scss']
})

export class ExlInputComponent {
}
